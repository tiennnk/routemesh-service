export const TRIP_PATTERNS = {
  CREATE: 'trip.create',
  FIND_BY_ID: 'trip.find_by_id',
  FIND_BY_RIDER: 'trip.find_by_rider',
  ACCEPT: 'trip.accept',
  UPDATE_STATUS: 'trip.update_status',
  CANCEL: 'trip.cancel',
} as const;
